<h1>画像投稿フォーム</h1>
<%= form_with model: @book do |f| %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>タイトル</h4>
  <%= f.text_field :name %>
  <h4>あらすじ</h4>
  <%= f.text_area :description %>
  <%= f.submit '投稿' %>
<% end %>
